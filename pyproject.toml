[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gan"
version = "0.1.0"
description = "GAN and WGAN-GP training utilities"
dependencies = ["torch", "numpy", "tqdm"]

[tool.setuptools.packages.find]
where = ["."]
